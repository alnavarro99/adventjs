type lights = '🔴' | '🟢'

export const adjustLights = (lights: lights[]): number => {
  // Code here
  return 0
}

export const changeLights = (lights: lights[]): lights[] => {
  // Code here
  return []
}

export const createLights = (light: lights, length: number): lights[] => {
  // Code here
  return []
}
